#pragma once

// NodeId / nullNodeId and the handle-table primitives
// (allocNode/resolveNode/retainNode/dropNode) are declared in
// <wasm-dom/vnode.hpp> so VNode can call them inline.
#include <wasm-dom/vnode.hpp>
